class Solution {
    public int[] sortedSquares(int[] A) {

        for(int i = 0; i < A.length; i++){
            int num = A[i];
            int sq = num * num;
            A[i] = sq;
        }
            Arrays.sort(A);

   return A;
        }
}
