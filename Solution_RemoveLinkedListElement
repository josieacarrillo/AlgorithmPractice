class Solution {
    public ListNode removeElements(ListNode head, int val) {
        ListNode prev = null;
       ListNode curr = head;
        ListNode next = curr.next;

            if( curr.val == val ){
                prev.next = next;
                curr = next;
                next = curr.next;
            }

        return head;
    }
}
